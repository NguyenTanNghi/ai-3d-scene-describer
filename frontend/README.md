# Frontend

React + TypeScript + Vite frontend for AI 3D Scene Describer.

```bash
npm install
npm run dev
```

Default API base URL is `http://localhost:5000/api`. Override it with `VITE_API_BASE_URL` in `.env`.
